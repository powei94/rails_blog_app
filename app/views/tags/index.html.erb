<h1>Tags</h1>

<ul id="tags">
    <%@tags.each do |tag|%>
    <li>
        <%= link_to tag.name, tag_path(tag)%>
        <% if logged_in?%>
            <%= link_to 'Eliminar', tag, method: :delete, data: { confirm: '¿Seguro que deseas borrar este tag?' } %>
        <% end %>
    </li>
    <%end%>
</ul>