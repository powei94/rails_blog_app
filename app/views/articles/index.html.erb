<h1>Artículos</h1>

<ul id="articles">
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article) %>
    </li>
  <% end %>
</ul>

<%= link_to "Crear nuevo artículo", new_article_path, class: "new_article" %>